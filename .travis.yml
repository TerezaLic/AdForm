language: bash


services:
  - docker
  
script:
  - docker build --tag=my-application .
  - cd $TRAVIS_BUILD_DIR
  - chmod +x ./deploy.sh


deploy:
  provider: script
  skip_cleanup: true
  script: chmod +x ./deploy.sh
  on:
    tags: true
