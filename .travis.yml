language: r
cache: packages

sudo: false

services:
  - docker

r_github_packages:
 - keboola/r-application

deploy:
  provider: script
  skip_cleanup: true
  script: ./deploy.sh
  on:
    tags: true
