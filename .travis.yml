language: r
cache: packages

sudo: false

services:
  - docker

r_github_packages:
 - keboola/r-application

script:
  - docker build --tag=my-application .

deploy:
  provider: script
  skip_cleanup: true
  script: ./deploy.sh
  on:
    tags: true
